export enum StorageKey {
  AppList = 'app_list',
  HumbleChoiceData = 'humble_choice_data',
  Options = 'options',
  OwnedGames = 'owned_games',
  SteamGames = 'steam_games',
}

export enum PageType {
  HumbleChoice = 'humbleChoice',
  HumbleGameBundle = 'humbleGameBundle',
}

export const USE_CACHING = true
export const ENABLE_DEBUG = true
